<!-- <?php
// if (isset($_POST['year'])) {
    // $selectedYear = $_POST['year'];

    // Database connection
    // $pdo = new PDO('mysql:host=localhost;dbname=pamanlinan_db', 'root', '', [
    //     PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION
    // ]);

    // Update age based on the selected year
//     $stmt = $pdo->prepare("
//         UPDATE people
//         SET age = TIMESTAMPDIFF(YEAR, date_of_birth, :selectedYear)
//         WHERE date_of_birth IS NOT NULL
//     ");
//     $stmt->execute(['selectedYear' => $selectedYear]);

//     echo "Age updated successfully for the year $selectedYear.";
// }
?> -->
